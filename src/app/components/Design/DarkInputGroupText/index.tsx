import InputGroup from 'react-bootstrap/InputGroup';
import styled from 'styled-components/macro';

export default styled(InputGroup.Text)`
  background: var(--clr-background) !important;
  color: var(--clr-white) !important;
`;
